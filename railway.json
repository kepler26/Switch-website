{
  "build": {
    "rootDirectory": "website"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn Switch_Webpage.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
